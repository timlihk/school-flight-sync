[phases.setup]
nixPkgs = ["nodejs_20"]
# Explicitly exclude bun to prevent automatic detection
excludePkgs = ["bun"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview"