[phases.setup]
nixPkgs = ["nodejs_20"]
# Explicitly exclude bun to prevent automatic detection
excludePkgs = ["bun"]
env = ["NODE_VERSION=20", "NIXPACKS_NODE_VERSION=20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview"