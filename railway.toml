[build]
  builder = "NIXPACKS"

[build.config]
  # Force Node.js 20 for Nixpacks - updated 2025-12-14
  NODE_VERSION = "20"
  NIXPACKS_NODE_VERSION = "20"

[deploy]
  startCommand = "npm run preview"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[environments.production.variables]
  NODE_ENV = "production"
  NIXPACKS_NODE_VERSION = "20"
